# Source-only checks. Functional and hardware verification remain separate.
minimum_pre_commit_version: '3.5.0'
repos:
  - repo: local
    hooks:
      - id: repository-hygiene
        name: Repository source hygiene
        entry: python scripts/repo_hygiene.py
        language: system
        pass_filenames: false
        always_run: true
